import {StyleSheet} from 'react-native';
import {scale} from '../../styles/scale.style';

export const MemberListStyle = StyleSheet.create({
    container: {
        flex: 1,
    },
    scrollview: {
        flex: 1,
        padding: scale(16),
    },
});

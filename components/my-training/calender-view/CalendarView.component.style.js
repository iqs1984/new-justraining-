import {StyleSheet} from 'react-native';
import theme from '../../../styles/theme.style';

export const CalendarViewComponentStyle = StyleSheet.create({
    container: {
        backgroundColor: theme.PRIMARY_VIEW,
    },
});

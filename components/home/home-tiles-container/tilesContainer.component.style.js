import {StyleSheet} from 'react-native';
import {scale} from '../../../styles/scale.style';

export const TilesContainerStyle = StyleSheet.create({
    container: {
        flexDirection: 'row',
        width: '100%',
        // flex: 1,
        flexWrap: 'wrap',
        justifyContent: "center"
    },
});
